rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // User-scoped uploads: uploads/{uid}/{sessionId}/{file}
    match /uploads/{uid}/{sessionId}/{fileName} {
      allow read, write, delete: if request.auth != null && request.auth.uid == uid;
    }
  }
}
